% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Bmsm_rolling.R
\name{Bmsm_rolling}
\alias{Bmsm_rolling}
\title{Rolling/Expanding Window Estimation for the Bivariate MSM Model.}
\usage{
Bmsm_rolling(
  ret,
  window,
  h = 1,
  type = c("rolling", "expanding"),
  kbar = 1,
  n = 252,
  s.err = FALSE
)
}
\arguments{
\item{ret}{Two-column matrix of returns.}

\item{window}{Integer >= 10. Fixed window length (rolling) or minimum window
length (expanding).}

\item{h}{Integer >= 1. Forecast horizon applied at the end of each window.}

\item{type}{One of \code{"rolling"} (fixed-width) or \code{"expanding"}
(growing from start). Default \code{"rolling"}.}

\item{kbar}{Number of MSM frequency components. Default 1.}

\item{n}{Integer. Trading days per year. Default 252.}

\item{s.err}{Logical. Compute standard errors? Default \code{FALSE}
(expensive in rolling context).}
}
\value{
A named list with two data frames:
\describe{
  \item{parameters}{One row per window: \code{window_end}, \code{m01},
    \code{m02}, \code{sigma1}, \code{sigma2}, \code{gammak}, \code{b},
    \code{rhoe}, \code{lambda}, \code{rhom}, \code{converged}.}
  \item{forecasts}{One row per window: \code{window_end}, \code{h},
    \code{vol1}, \code{vol2}, \code{covt}, \code{rho.t}.}
}
}
\description{
Re-fits \code{\link{Bmsm}} on successive rolling or expanding windows and
returns estimated parameters and h-step-ahead forecasts for each window.
}
\examples{
data("calvet2006returns")
ret <- as.matrix(calvet2006returns[, 2:3]) * 100
ret_sub <- ret[1:60, ]
result <- Bmsm_rolling(ret_sub, window = 50, h = 1, kbar = 1)
head(result$parameters)
head(result$forecasts)

}
