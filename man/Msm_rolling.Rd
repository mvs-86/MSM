% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Msm_rolling.R
\name{Msm_rolling}
\alias{Msm_rolling}
\title{Rolling/Expanding Window Estimation for the Univariate MSM Model.}
\usage{
Msm_rolling(
  ret,
  window,
  h = 1,
  type = c("rolling", "expanding"),
  kbar = 1,
  n.vol = 252,
  nw.lag = 0
)
}
\arguments{
\item{ret}{Column matrix of returns.}

\item{window}{Integer >= 10. Fixed window length (rolling) or minimum window
length (expanding).}

\item{h}{Integer >= 1. Forecast horizon applied at the end of each window.}

\item{type}{One of \code{"rolling"} (fixed-width) or \code{"expanding"}
(growing from start). Default \code{"rolling"}.}

\item{kbar}{Number of MSM frequency components. Default 1.}

\item{n.vol}{Trading days per year. Default 252.}

\item{nw.lag}{Newey-West lags for standard errors. Default 0.}
}
\value{
A named list with two data frames:
\describe{
  \item{parameters}{One row per window: \code{window_end}, \code{m0},
    \code{b}, \code{gammak}, \code{sigma}, \code{converged}.}
  \item{forecasts}{One row per window: \code{window_end}, \code{h},
    \code{vol}, \code{vol.sq}.}
}
}
\description{
Re-fits \code{\link{Msm}} on successive rolling or expanding windows and
returns estimated parameters and h-step-ahead forecasts for each window.
}
\note{
Warm-starting is only effective when \code{kbar >= 2}. When
  \code{kbar = 1}, \code{\link{Msm}} sets the \code{b} parameter to
  \code{NA} (it is unused in a single-component model). Passing an \code{NA}
  parameter vector as \code{para0} would crash \code{Msm_parameter_check},
  so each window is re-initialized from scratch for \code{kbar = 1}.
}
\examples{
data("calvet2004data")
ret <- na.omit(as.matrix(calvet2004data$caret)) * 100
ret_sub <- ret[1:60, , drop = FALSE]
result <- Msm_rolling(ret_sub, window = 50, h = 1, kbar = 1)
head(result$parameters)
head(result$forecasts)

}
